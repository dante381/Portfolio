# SECURITY.md

Security checklist for the portfolio + analytics dashboard. All items tracked against the security spec in the plan.

## Threat Model

**Public surface**: Portfolio pages (anonymous traffic).  
**Private surface**: `/dashboard`, `/api/stats` — single admin only.  
**Risks**: PII leakage of visitors, admin account takeover, injection, abuse of ingest endpoint, secret leakage.

---

## Security Checklist

| #    | Item                                                                                                                                                                                                                                                                                                  | Status  | Phase                                                  | Notes                                                                                                                                                                                                               |
| ---- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------- | ------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| S-01 | **Secrets management** — all secrets in `.env.local` (gitignored). `.env.example` documents names only. Verify `git status` never shows `.env.local`. Vercel envs set in dashboard, not code.                                                                                                         | DONE    | Phase 0 (infra) / Phase 5 (Vercel)                     | `.env*` in `.gitignore`. `git check-ignore .env.local` confirmed. Migration SQL has no secrets. DATABASE_URL/HASH_SALT in `.env.local` only.                                                                        |
| S-02 | **PII minimization** — never store raw IP or raw UA. Store only `visitor_hash` (sha256(IP + UA + daily-rotating salt)). Geo limited to country/city from Vercel edge headers. Respect `DNT`/`Sec-GPC`. Add privacy note in footer.                                                                    | DONE    | Phase 2                                                | `analytics.ts` implements `visitorHash()` (sha256 daily-rotating), `isDoNotTrack()`. Route never persists raw IP/UA. Unit test "does NOT include raw ip or ua" + "stores referrer_host only" assertions green.     |
| S-03 | **Auth hardening** — password stored as bcrypt hash (cost 12) in env. JWT (`jose`) HS256 with 256-bit secret, 2h expiry, httpOnly + Secure + SameSite=Strict cookie. No token in localStorage. Logout clears cookie. Login rate-limited (5/15min/IP) + generic error msg + constant-time delay.       | DONE    | Phase 3                                                | `src/lib/auth.ts`: `hashPassword`/`verifyPassword` (bcrypt cost 12), `signSession`/`verifySession` (jose HS256, 2h), `verifyAdminCredentials` (constant-time — always runs bcrypt compare, even for unknown users, to prevent timing oracle). `scripts/gen-admin.mjs` generates and stores bcrypt hash in `.env.local`. `/api/auth/login`: rate-limited (5/15min/IP, `authLimit`), Origin/Referer same-site check, generic error, 300ms fixed delay on failure, httpOnly+SameSite=Strict cookie set on success. `/api/auth/logout` clears cookie. Unit tests: sign/verify round-trip, expired-token rejection, tampered-token rejection, verifyPassword true/false, unknown-user timing path. Integration tests: 200+cookie on valid creds, 401 generic on bad creds, 429 on brute-force, 403 on bad origin. Cookie-flags assertions green. |
| S-04 | **Authorization** — middleware.ts verifies JWT for `/dashboard/*` and `/api/stats`. `/api/stats` re-checks server-side. Unauth → 401/redirect to /login.                                                                                                                                              | DONE    | Phase 3                                                | `middleware.ts` (edge-compatible): guards `/dashboard/:path*` → redirect to `/login?next=<path>`; guards `/api/stats` → 401 JSON. Uses `jose.jwtVerify` directly (no Node.js crypto). `dashboard/page.tsx` and `/api/stats/route.ts` both call `getSession()` server-side as defense-in-depth (plan spec S-04). Middleware unit tests: 307 redirect for unauth /dashboard with `next=` param, 401 for unauth /api/stats, 200 passthrough for authenticated requests, no-gate on public /api/track. |
| S-05 | **Input validation** — zod parse every API body/query. Reject unknown fields. Clamp string lengths. `/api/track` accepts only enum types + whitelisted section names.                                                                                                                                 | DONE    | Phase 2                                                | `validation.ts` — `trackPayloadSchema` uses `.strict()`, enum types, whitelisted section IDs, max 512-char path. 14 validation unit tests green (rejects unknown keys, bad types, oversized, SQL injection attempt). |
| S-06 | **Injection prevention** — Drizzle parameterized queries only (no raw SQL string concat). No `dangerouslySetInnerHTML` with untrusted data.                                                                                                                                                           | DONE    | Phase 2                                                | All DB writes use Drizzle `insert().values()` and `update().set()` with typed objects — no raw SQL string concatenation. Schema reviewed in Phase 4.                                                                |
| S-07 | **XSS / security headers** — CSP, HSTS (max-age 1y + includeSubDomains + preload), X-Frame-Options DENY, X-Content-Type-Options nosniff, Referrer-Policy strict-origin-when-cross-origin, Permissions-Policy (camera/mic/geo disabled). Disable `x-powered-by`. `productionBrowserSourceMaps: false`. | DONE    | Phase 0 (headers skeleton) / Phase 4 (CSP tightened)   | Phase 4: CSP tightened — `script-src 'self'` (removed `unsafe-inline` and `unsafe-eval`); `style-src 'self' 'unsafe-inline'` retained (Tailwind + Framer Motion inline styles); `frame-ancestors 'none'`; `object-src 'none'`; `base-uri 'self'`; `form-action 'self'`. Header assertion tests added in `security-headers.test.ts` (13 assertions, all green). `x-powered-by` disabled. `productionBrowserSourceMaps: false`. |
| S-08 | **CSRF** — state-changing routes are POST + SameSite=Strict cookie. Login/logout check `Origin`/`Referer` host. `/api/track` validated + rate-limited + Origin-checked.                                                                                                                               | DONE    | Phase 2 (track) / Phase 3 (auth)                       | `/api/track` `isSameOrigin()` checks Origin/Referer host against request host; returns 403 on mismatch. Integration test "returns 403 for cross-origin" green. Auth routes in Phase 3.                              |
| S-09 | **Rate limiting / abuse** — `/api/track` 60 req/min/IP; `/api/auth/login` 5/15min/IP. Reject oversized bodies. Drop malformed/oversized payloads.                                                                                                                                                     | DONE    | Phase 2 (track) / Phase 3 (auth)                       | `ratelimit.ts`: Upstash sliding-window 60/60s when env vars present; in-memory fallback for local. 2KB body guard (content-length + text.length). Returns 429 on rate-limit exceed, 413 on oversize. Tests green.  |
| S-10 | **Dependency hygiene** — `npm audit --audit-level=high` in CI. Dependabot weekly. Pin versions. Remove unused deps.                                                                                                                                                                                   | PARTIAL | Phase 0                                                | CI audit gate added. Dependabot configured. 6 moderate-only findings logged in ISSUES.md (I-002). Added: ua-parser-js, @upstash/ratelimit, @upstash/redis. No new high/critical findings.                           |
| S-11 | **Error handling** — never leak stack traces or DB errors to client. Generic 4xx/5xx JSON. Log details server-side only.                                                                                                                                                                              | DONE    | Phase 2                                                | `/api/track` wraps all DB calls in try/catch; `console.error` logs server-side only. Client receives generic {"error":"Internal server error"} with status 500. No stack trace in response.                         |
| S-12 | **Cookie/secret rotation** — document process for rotating `AUTH_SECRET` and `HASH_SALT`.                                                                                                                                                                                                             | TODO    | Phase 5                                                | Rotation procedure: (1) generate new value, (2) update Vercel env var, (3) redeploy. Old JWTs/hashes immediately invalid — users re-authenticate. Daily HASH_SALT rotation is by design (unlinks cross-day hashes). |
| S-13 | **GitHub repo safety** — public repo contains NO secrets, NO real visitor data dumps. `.env*`, DB dumps, `*.local` gitignored. Verified before first push.                                                                                                                                            | PARTIAL | Phase 0 / Phase 5                                      | .gitignore updated. Verify with `git status` before Phase 5 push.                                                                                                                                                   |

---

## Secret rotation procedure (S-12)

- **AUTH_SECRET**: Generate new 256-bit value (`openssl rand -base64 32`). Update in Vercel Dashboard. Redeploy. All existing JWT sessions immediately invalidated — admin must re-login.
- **HASH_SALT**: Rotates daily by design (analytics.ts reads `HASH_SALT + date`). To permanently rotate: update env var + redeploy. Cross-day continuity is not expected (privacy by design).
